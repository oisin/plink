source :rubygems

gem 'rack'
gem 'rack-throttle'
gem 'sinatra'
gem 'json'
gem 'bson_ext'
gem 'mongo_mapper'
gem 'memcached'
gem 'SystemTimer'

# This is to prevent CloudFoundry from using WEBrick
gem 'thin'

group :test, :develop do
  gem 'ruby-debug'
end

group :test do
  gem 'rack-test'
end
